import preset from './tailwind.preset';

export { palette, radius, motion, breakpoints, fontWeights } from './tokens';
export type { Palette, Radius, Motion, Breakpoints } from './tokens';

export default preset;
